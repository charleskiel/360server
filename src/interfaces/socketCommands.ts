
export namespace SocketCommands {
	export enum CommandType {
		login,
	}
	export enum EventType {
		ContentController,
		CasparCommand,
		Channel,
		NowPlaying,
	}
	export enum DataType {
		NowPlaying,
		NowPlayingList,

	}
}